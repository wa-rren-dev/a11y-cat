import React from "react";
import { Grid, GridItem } from "@nice-digital/nds-grid";

export const Header = props => (
  <Grid>
    <GridItem cols={12}>
      <h1>A11y-Cat</h1>
    </GridItem>
  </Grid>
);
